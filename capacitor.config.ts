import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'babel.testSanitas.app',
  appName: 'test_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
